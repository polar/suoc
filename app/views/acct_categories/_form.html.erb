<% form_for(@category, :html => { :class => "MainForm" }) do |f| %>
  <%= f.error_messages %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :description %>
  <%= f.text_area :description,
         {:size => "86x5", :class => "rich_text_editor"} %>
  <p>
    <%= f.submit @submit %>
  </p>
<% end %>
