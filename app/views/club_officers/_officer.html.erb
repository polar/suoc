<tr>
 <td><%= officer.office.name %></td>
 <td><%= officer.start_date.strftime("%m-%d-%Y") %></td>
 <td><%= officer.end_date.strftime("%m-%d-%Y") %></td>
 <td> <%= button_to "Delete",
                    url_for( :action => :delete_officer,
                             :id => officer),
                    :confirm => "Are you sure you want to DELETE this officership entry, erasing the record forever?"  %></td>
</tr>