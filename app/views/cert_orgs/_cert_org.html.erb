<%
# This partial renders the editor for the CertOrg
# locals: 
#   cert_org
%>
<tr>
  <td><%= link_to cert_org.name, cert_org_path(cert_org) %></td>
  <td><%= cert_org.cert_type.name %></td>
  <td><%= cert_org.description %></td>
  <td><%= button_to "Edit", edit_cert_org_path(cert_org), 
                    :method => :get %></td>
  <td><%= button_to "Delete", cert_org_path(cert_org), 
                    :method => :delete, :confirm => "Are you sure?" %></td>
<tr>
